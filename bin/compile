BUILD_DIR="$1"
CACHE_DIR="$2"

if [ ! -d $CACHE_DIR/$1-$PYODIDE_VERSION ]; then
   rm -rf $CACHE_DIR
   mkdir -p $CACHE_DIR
   ./bin/install_pyodide $CACHE_DIR/pyodide
   python -m whitenoise.compress $CACHE_DIR
fi

mkdir -p $BUILD_DIR/build
cp -r $CACHE_DIR/ $BUILD_DIR/build